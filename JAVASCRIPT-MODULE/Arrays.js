 // Arrays are speccial which hold more than one value.
 //Arrays starts indexing numbers from zero

 let numbers = [1, 2, 3, 4, 5];
 console.log(numbers);

 let fruits = ["fruit", "apples", "banana"];
 console.log(fruits);

 //Modify

 let myArray = [1, 2, 3, 4, 5 , 6, 7,];
 


 //Adding arrays

 let cars = new Array("Ford", "toyota", "mercedes");
 cars.push("ferari");
 console.log(cars);
 
 //Removing arrays

 //pop. it removes the last item

 let fruit = ["Apples", "bananas", "grapes","mangoes"];
 fruits.pop();
 console.log(fruits);

 let itemRemoved = fruits.shift(; )
 // shift removes the first item

 let itemRemoved = fruits.shift();

  